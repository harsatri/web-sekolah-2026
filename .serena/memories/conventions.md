# Development Conventions
- Use ES Modules (`import`/`export`).
- Backend uses `express` and `mysql2`.
- Frontend uses `react` with `vite`.
- Environment variables are managed via `.env` files in `backend/`.
