# Implementation Plans

Generated by the improve skill on 2026-07-08. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                   | Priority | Effort | Depends on | Status |
|------|---------------------------------------------------------|----------|--------|------------|--------|
| 001  | Cap per-container resource ceiling (swap/pids/fd/init) | P1       | S      | —          | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 001 has no dependencies; it is a single-file (`docker-compose.yml`) additive
  change and can execute immediately.

## Findings considered and rejected

- **Per-container block-I/O (blkio) and network bandwidth throttling**: not
  doable in Compose (non-Swarm); requires daemon/cgroup-level config. Out of
  scope for a compose-only hardening pass.
- **Logging rotation**: already capped (`json-file`, `max-size`/`max-file`) on
  all three services. No change needed.

## Findings deferred (not yet planned — available on request)

- **Finding 6 — `restart: unless-stopped` crashes loop with no backoff**: real
  crash-loop CPU risk, but Compose has no clean max-retry/backoff without
  Swarm; changing to `on-failure` trades the loop for a service that stays down
  when a flapping dependency misbehaves. Needs a deliberate decision before
  planning.
- **Finding 7 — `uploads_data` volume grows unbounded (host disk fill)**: the
  largest remaining host-wedge risk, but the fix is app-level (retention/cleanup
  of `/app/uploads`) plus monitoring, not a compose change. Plan on request.
- **Finding 8 — committed `backend/.env` credentials**: out of the throttle
  scope of this pass but HIGH severity; should be rotated and moved to a
  non-committed secret path. Plan on request.
